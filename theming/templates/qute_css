/* -*- mode: css -*- */

* {
  scroll-behavior: auto !important;
}

::selection {
	/* Set background color for selected text */
	background: {{.color.focused.background}};
	color: {{.color.focused.foreground}};
}
