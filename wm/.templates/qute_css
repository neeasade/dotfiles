/* -*- mode: css -*- */

* {
	    scroll-behavior: auto !important;
}

body * {
  font-family: "{{font.variable.family}}" !important;
}

div[class^='Box'],
div[class^='Box'] *,
div[class^='code'],
div[class^='code'] *,
code, code *,
pre, pre *,
kbd, kbd *,
samp, samp * {
	font-family: "{{font.mono.family}}" !important;
}

::selection {
	/* Set background color for selected text */
	background: {{color.focused.background}};
	color: {{color.focused.foreground}};
}

