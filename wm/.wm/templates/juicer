[bar]
line={{p_line}}
padding={{p_padding}}
bg = "{{p_bg_normal}}"
fg = "{{p_fg_normal}}"
format = "{{p_format}}"
fonts = "{{p_font_main}}"
        "{{p_font_icon}}"
        "font-linux-10"
class = "{{p_window_class}}"
offset = {{p_offset}}
delimiter= "{{p_delimiter}}"

; todo: consider calculating this at runtime?
clickables = 40

