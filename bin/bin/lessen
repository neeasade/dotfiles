#!/usr/bin/env elisp

(let ((color (concat "#" (first ns-args)))
      (percent (string-to-number (second ns-args))))
  (if (ns/color-is-light-p color)
      (color-darken-name color percent)
    (color-lighten-name color percent)))
