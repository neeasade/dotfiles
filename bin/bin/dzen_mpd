#!/bin/bash

. ~/.bspwm_theme

width=300
length=10

lines=5
format="%position% [[%artist% - ]%title%]|[%file%]"
format="%position% %title%"
(
echo "Playlist"
mpc playlist -f "$format" | grep -$lines "$(mpc current -f %position%)" | while read -r position song; do
echo " ^ca(1, mpc play $position) $song ^ca() "
done
) | eval dzen2 $(dzen_options)
