#!/bin/bash
# dzen2 bspwm_theme menu

. ~/.bspwm_theme

width=100
length=`ls ~/.config/bspwm/themes | wc -l`
length=`expr $length - 2`

THEME=`ls ~/.config/bspwm/themes | grep -v base | sed s/.bspwm_theme// | eval dzen2 $(dzen_options)`

[[ ! -z $THEME ]] && ltheme $THEME
