#!/usr/bin/env sh
# always run mpv w/ socket

mpv="$(type -a mpv | tail -n 1 | sed 's/.* //')"

# todo: account for multiple instances here
mpc pause >/dev/null

$mpv --input-ipc-server=/tmp/mpvsocket "$@"
