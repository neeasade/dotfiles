#!/bin/sh
# this is just a wrapper for accessing the current theme through gott

theme_dir="$HOME/.dotfiles/theming/themes"

gott \
    -t "${theme_dir}/colors.toml" \
    -t "${theme_dir}/defaults.toml" \
    -t "${theme_dir}/current.toml" \
    "$@"

