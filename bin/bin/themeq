#!/bin/sh
# speed

mkdir -p "$HOME/.cache/rice"

if [ "$1" = "REFRESH" ]; then
    theme -s > "$HOME/.cache/rice/theme_cache.sh"
    exit $?
fi

. "$HOME/.cache/rice/theme_cache.sh"
eval printf '%s' "\$$1"
