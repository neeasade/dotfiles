#!/usr/bin/env elisp
;; make dark colors darker
;; make light colors lighter

(ns/let-script-args (percent color)
		    (let ((color (concat "#" color))
			  (percent (string-to-number percent)))
		      (ns/color-greaten percent color)))
