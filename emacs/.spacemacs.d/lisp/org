;; -*- mode: emacs-lisp -*-

(load-settings "org" '(
  bullets-bullet-list '("@" "%" ">" ">")
  directory "~/org"
  startup-indented t
  todo-keywords '((type "TODO" "NEXT" "WAITING" "DONE"))
  blank-before-new-entry '((heading . t) (plainlist-item . nil))
  ellipsis "…"
  clock-x11idle-program-name "x11idle"
  clock-idle-time 10
  clock-sound nil
  pomodoro-play-sounds nil
  ))
